import 'package:flutter/material.dart';

class WarrantyDetailsCard extends StatelessWidget {
  const WarrantyDetailsCard({super.key}) : super();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('data'),
    );
  }
}
